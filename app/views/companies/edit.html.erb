<% provide(:title, "Edit Company") %>

<div class="form_wrapper company-form-panel">
  <div class="panel panel-primary">
    <div class="panel-heading">
      <h4>
        Edit Company
      </h4>
    </div>
    <div class="panel-body">
      <%= render "form" %>

      <br>
      <%= link_to companies_path, class: "btn btn-primary back-button" do %>
        <span class="glyphicon glyphicon-chevron-left"></span> Back to Companies
      <% end %>
    </div>
  </div>
</div>
